(define (plan test_plan)
(:domain main_domain) 
(:problem main_problem)  ;
;;(:planvars )
;;(:init )  ;; this is for initialize plan variable
 
(:body
   (sequence
          (subplan ( a_plan ) );;end-sub-plan
   );; end-sequence
);; end-body
) ;;end define
