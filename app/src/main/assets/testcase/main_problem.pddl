(define (problem pb1)
   	(:domain rb)
   	
   	(:objects
              )
              
	(:init 
	
        (A)
	)   
	
	;;NOTE: The number of goals should be exactly the same as in the list of a node.
	(:goal (AND 
                (C)
           )
	);end-goal
)